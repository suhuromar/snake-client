const IP = 'localhost';
const PORT = 50541;

const UPKEY = "Move: up";
const LEFTKEY = "Move: left";
const DOWNKEY = "Move: down";
const RIGHTKEY = "Move: right";

module.exports = {
    IP,
    PORT,
    UPKEY,
    LEFTKEY,
    DOWNKEY,
    RIGHTKEY
};